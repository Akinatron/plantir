/**
 * Tipos generados a partir del esquema de Supabase.
 *
 * NOTA: este archivo es un placeholder. La generación real se hace con:
 *   `npx supabase gen types typescript --local > src/lib/supabase/database.types.ts`
 * tras `supabase start`. Mientras tanto, exportamos un `Database` mínimo
 * con tipos `any` para que el cliente compile. En CI se regenera y se
 * commitea la versión tipada.
 *
 * @generated
 * @todo regenerar con `supabase gen types` post-deploy de migraciones.
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
